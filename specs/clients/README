# Módulo: Gestión de Clientes (Clients & KYC)

## 1. Resumen
Este módulo es responsable de la gestión de la identidad de los solicitantes de crédito. Implementa los lineamientos de "Conoce a tu Cliente" (KYC), asegurando que los datos personales estén validados, sean únicos y estén actualizados para la correcta evaluación crediticia.

## 2. Reglas de Negocio
*   **BR-CLI-001 (Unicidad):** El documento de identidad (`national_id`) y el correo electrónico (`email`) deben ser estrictamente únicos en todo el sistema.
*   **BR-CLI-002 (Mayoría de Edad):** El cliente debe ser mayor de 18 años al momento del registro.
*   **BR-CLI-003 (Corrección de Identidad):** El documento de identidad (`national_id`) no se edita junto con el resto de los datos del cliente -- tiene su propia acción (`UpdateNationalId`) que registra el valor anterior y el nuevo en el `AuditLog`. Requiere el mismo rol que ya edita clientes (`CREDIT_ANALYST_AND_ABOVE`, ver `rbac.py`) -- hasta 2026-09 era exclusiva de Administrador, pero se relajó por decisión del negocio: quien puede corregir nombre, dirección o referencias también puede corregir un documento mal tipeado, sin escalar. Nombre y apellido (`first_name`/`last_name`) son editables directamente vía `UpdateClient`, igual que el resto de los datos de contacto.
*   **BR-CLI-004 (Historial):** No se pueden eliminar clientes (Soft Delete o inhabilitación temporal únicamente) si tienen préstamos vinculados en cualquier estado que no sea "PAID" (Pagado).
*   **BR-CLI-005 (Referencias obligatorias):** Todo cliente debe registrar dos referencias personales (nombre, parentesco/relación, teléfono) y una referencia laboral (empleador, cargo, teléfono, antigüedad) al momento del alta; son editables luego vía `UpdateClient`, pero nunca pueden quedar vacías.
*   **BR-CLI-006 (Origen de fondos obligatorio):** Todo cliente debe registrar el origen de sus fondos (texto libre, ej. "Salario", "Negocio propio") al momento del alta; es editable luego vía `UpdateClient`, pero nunca puede quedar vacío.
*   **BR-CLI-007 (Perfil extendido) -- ELIMINADO.** El perfil extendido opcional (vencimiento de cédula, estado civil, nivel de estudios, ocupación, barrio, calificación de riesgo, sector económico) se retiró del sistema por decisión de producto: ninguno de esos campos era obligatorio ni intervenía en una regla de negocio, y alargaban el formulario de alta sin contrapartida. Las columnas se eliminaron (migración `c7a1d4e5f6b2`) y los números de campo que ocupaban quedaron `reserved` en `protos/client_service.proto`. El identificador BR-CLI-007 no se reutiliza.
*   **BR-CLI-008 (Eliminación real de un cliente):** `DeleteClient(client_id, reason)` borra definitivamente la fila del cliente -- a diferencia de `DeactivateClient` (BR-CLI-004), que solo inhabilita. Por decisión explícita del negocio se puede eliminar **cualquier** cliente, sin excepción por el estado de sus préstamos: `PENDING`/`APPROVED`/`EXPIRED`/`ACTIVE`/`PAID`/`DEFAULTED` se aceptan todos por igual (a diferencia de `DeleteLoan`, BR-LOAN-012, que excluye los tres últimos porque implican dinero ya movido). Esto obliga a borrar en cascada, dentro de la misma transacción: los préstamos del cliente, sus `LoanPayment` y sus `LoanInstallmentAdjustment`. Un `CashMovement` que haya quedado imputado a uno de esos pagos (BR-CAJA-004) **no se borra** -- solo se le limpia `loan_payment_id` a null -- porque ese movimiento puede estar sumado en el arqueo de un turno de caja ya cerrado y firmado (BR-CAJA-003), y borrarlo cambiaría en silencio un cierre ya firmado. Mismo rol que originar/editar un cliente (`CREDIT_ANALYST_AND_ABOVE`, ver `rbac.py`) y mismo criterio que `DeleteLoan`: quien puede cargar un cliente es quien nota el error y no necesita escalar para deshacerlo. `reason` es obligatorio: la fila desaparece y el `AuditLog` (`CLIENTE_ELIMINADO`) es lo único que queda para explicar por qué.

## 3. Modelo de Datos (PostgreSQL)
**Tabla: `clients`**
*   `id` (UUID, PK)
*   `first_name` (VARCHAR)
*   `last_name` (VARCHAR)
*   `national_id` (VARCHAR, UNIQUE) - Documento Nacional de Identidad
*   `date_of_birth` (DATE)
*   `email` (VARCHAR, UNIQUE)
*   `phone_number` (VARCHAR)
*   `address` (TEXT)
*   `is_active` (BOOLEAN) - Para soft deletes
*   `personal_reference_1_name`, `personal_reference_1_relationship`, `personal_reference_1_phone` (VARCHAR) - Referencia personal 1 (BR-CLI-005)
*   `personal_reference_2_name`, `personal_reference_2_relationship`, `personal_reference_2_phone` (VARCHAR) - Referencia personal 2 (BR-CLI-005)
*   `employment_reference_employer`, `employment_reference_position`, `employment_reference_phone`, `employment_reference_seniority` (VARCHAR) - Referencia laboral (BR-CLI-005)
*   `source_of_funds` (VARCHAR) - Origen de fondos (BR-CLI-006)
*   `created_at` (Timestamp)
*   `updated_at` (Timestamp)

## 4. Contratos gRPC Relacionados (`protos/client_service.proto`)
*   `CreateClientRequest` / `CreateClientResponse`: Registro de un nuevo cliente.
*   `GetClientByIdRequest` / `GetClientByIdResponse`: Obtiene el perfil completo y estado del cliente.
*   `SearchClientsRequest` / `SearchClientsResponse`: Búsqueda paginada por nombre, documento o teléfono.
*   `UpdateClientRequest` / `UpdateClientResponse`: Actualización de datos de contacto.
*   `DeactivateClientRequest` / `DeactivateClientResponse`: Inhabilitación (soft delete) -- BR-CLI-004.
*   `UpdateNationalIdRequest` / `UpdateNationalIdResponse`: Corrección del documento de identidad -- BR-CLI-003.
*   `DeleteClientRequest` / `DeleteClientResponse`: Eliminación real y en cascada de un cliente -- BR-CLI-008.